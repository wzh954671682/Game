# 《掌上英雄 V1.0》项目开发完成度预览

> 统计基准：逐条对比《项目开发大纲细则》与《开发流程与里程碑》的全部可执行条目
> 统计日期：2026-05-07
> 状态说明：✅ 已完成 | ⚠️ 部分完成 | ❌ 未完成

---

## 一、按《开发流程与里程碑》Phase 划分

### Phase 1: 基础设施与数据契约 (Foundation)

| 状态 | 编号 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ✅ | Task 1.1 | 全局信号总线 (GameEvents.gd) | — | `Scripts/Global/GameEvents.gd`，Autoload 单例，4 个强类型信号（详见 §七） |
| ✅ | Task 1.2 | JSON 数据加载器 (DataManager.gd) | — | `Scripts/Global/DataManager.gd`，Autoload，含 `file_exists` → `JSON.parse_string` → null 判定的完整安全链路 |
| ✅ | Task 1.3 | 棋盘坐标映射器 (GridManager.gd) | — | `Scripts/Logic/GridManager.gd`，Autoload，5x5 `Vector2i` ↔ `Vector2`，Safe Area 偏移，`_clamp_grid` 越界保护 |

> Phase 1 完成度：**3/3 (100%)**

---

### Phase 2: 战斗实体与核心判定 (Battle MVP)

| 状态 | 编号 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ⚠️ | Task 2.1 | 怪物基础移动 (Enemy.gd) | (1) `_physics_process` 向下移动 (2) 受击减速 (3) 到达底线触发 `wall_hit` 信号 (4) 死亡动画 | ✅ (1)(3) 已实现；❌ (2) 无减速接口；❌ (4) 直接 `queue_free()`，无动画、未发射 `enemy_died` 信号 |
| ⚠️ | Task 2.2 | 英雄拦截系统 (Hero.gd) | (1) `RayCast2D` 向上检测 (2) `block_count` 限制拦截数 (3) 0.1s 检测频率 (4) Idle/Intercepting 状态机 (5) 攻击动画与伤害输出 | ✅ (1)(2)(3) 已实现；❌ (4) 无状态机，`current_blocked_enemies` 只增不减；❌ (5) 检测到敌人后无伤害信号 |
| ⚠️ | Task 2.3 | 强制位移算法 (BattleManager) | (1) 递归挤压 (2) `TRANS_QUART` Tween (3) 连带判定触发拦截 | ✅ (1)(2) 已实现（`MAX_RECURSIVE_DEPTH=5`）；❌ (3) 大纲要求"触发所有重叠怪物的拦截逻辑"，当前仅做位移，未触发 Hero 拦截 |

> Phase 2 完成度：原评估过高，**实际约 60%** — 三个 Task 均存在关键行为缺失

---

### Phase 3: 卡牌系统与手牌逻辑 (Card Logic)

| 状态 | 编号 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ❌ | Task 3.1 | 手牌槽位管理 | (1) 5 槽位布局 (2) 拖拽状态切换 (3) `z_index` 提升 (4) 1.1x 缩放 + ±5° 倾斜反馈 | ❌ 全部未实现，无卡牌 UI 场景 |
| ✅ | Task 3.2 | 备用队列与死锁救援 | (1) FIFO 队列 (2) 0.1s `await` 补牌间隔 (3) 5s 开局静默期 (4) 10s CD (5) 5 张基础角色卡救援包 | ✅ (1)-(4) 完整实现；⚠️ (5) 代码写死 `"basic_hero"` 字符串，未读取 `battle_logic_config.json` 的 `package_content`（shielder_01 x2 + gunner_01 x3） |

> Phase 3 完成度：**1/2 (50%)** — 后端完整，前端为零，且救援卡池未数据驱动

---

### Phase 4: UI 表现与持久化 (UX & Meta)

| 状态 | 编号 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ✅ | Task 4.1 | 刘海屏 Safe Area 适配 | (1) `DisplayServer.get_display_safe_area()` (2) MarginContainer 动态 margin | ✅ (1)(2) 均实现（`SafeAreaContainer.gd`） |
| ⚠️ | Task 4.2 | 打击感增强 (VFX) | (1) Hit Stop (`Engine.time_scale`) (2) 屏幕震动 (Camera2D offset) | ✅ (1) 引用计数式 Hit Stop 已实现；❌ (2) `battle_logic_config.json` 有参数（intensity=5.0, 0.2s），但无 Camera2D 震动代码 |
| ✅ | Task 4.3 | 加密存档系统 (SaveManager) | (1) AES 加密 ConfigFile (2) 异常容错 | ✅ (1)(2) 均实现；⚠️ 存储内容偏少，仅 gold/max_level/unlocked_cards（详见 §七） |

> Phase 4 完成度：**2.3/3 (77%)**

---

## 二、按《项目开发大纲细则》逐章节划分

### 1. 项目综述与核心概念

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ✅ | 1.1.2 | 目标平台—设计分辨率 | 1080x2160 + Canvas Items Expand | `project.godot` 已配置 `viewport_width=1080`, `viewport_height=2160`, `stretch/mode=canvas_items`, `stretch/aspect=expand` |
| ✅ | 1.1.2 | 目标平台—Safe Area 获取 | `DisplayServer.get_display_safe_area()` 启动时获取 | `GridManager._refresh_layout()` 与 `SafeAreaContainer._ready()` 均调用 |
| ✅ | 1.1.2 | 目标平台—顶层 UI 容器化 | 所有交互 UI 包裹 MarginContainer | `SafeAreaContainer.gd` 继承 `MarginContainer`，自动设置四边 margin |
| ❌ | 1.1.2 | 目标平台—背景延伸原则 | 背景图 1242x2688 溢出渲染填满全屏 | 未实现，无战斗背景图资源 |
| ❌ | 1.1.2 | 目标平台—锚点策略 | 禁止绝对坐标，百分比锚点，弹窗 80% 居中 | 无 UI 场景可验证；BattleTest 使用 GridManager 坐标系统，符合棋盘层锚点原则 |
| ✅ | 1.1.3 | 技术选型—引擎版本 | Godot 4.x | `config_version=5` (Godot 4.6) |
| ✅ | 1.1.3 | 技术选型—渲染后端 | Compatibility 渲染模式 | `rendering_method=gl_compatibility`，`features=["4.6", "GL Compatibility"]` |
| ✅ | 1.1.3 | 技术选型—架构模式 | Resource-Based + Event Bus | 7 个 Autoload 单例 + 4 个 JSON 数据文件 |

> 注：§1.1.1 游戏定位、§1.2.1 局内循环、§1.2.2 局外循环为纯概念描述，无可执行代码条目，不纳入统计。

---

### 2. 战斗系统逻辑 (Battle System)

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ✅ | 2.1.1 | 5x5 棋盘格点坐标映射 | (1) 逻辑坐标 `Vector2i(0,0)`→`(4,4)` (2) 屏幕映射公式含中心偏移 (3) Safe Area 偏移 (4) 越界 clamp | ✅ (1)-(4) 全部实现（`GridManager.gd`） |
| ❌ | 2.1.2 | 角色拖拽反馈逻辑 | (1) `InputEventScreenTouch` 拖拽态切换 (2) Tween 1.1x 缩放 (3) 拖拽速度驱动 ±5° 倾斜 (4) `z_index` 提升悬浮 | ❌ 全部未实现（无卡牌场景） |
| ⚠️ | 2.1.3 | 强制位移判定流程 | (1) 怪物占据格位时触发位移(向上挤) (2) `TRANS_QUART` 平滑移动 (3) 连带判定：重叠格位仍位移 + 触发所有重叠怪拦截 | ✅ (1)(2) BattleManager 已实现；❌ (3) 大纲明确要求"随后触发所有重叠怪物的'拦截'逻辑"，当前仅做坐标位移，未调用 Hero 拦截 |
| ⚠️ | 2.2.1 | 纵向攻击检测机制 | (1) `RayCast2D` 或 `ShapeCast2D` 向上 (2) 固定频率检测（每帧或优化） (3) 检测成功→攻击动画 (4) 产生伤害信号→怪物 HP 扣除 (5) 高伤害触发 Hit Stop | ✅ (1)(2) `RayCast2D` + 0.1s；❌ (3) 无攻击动画；❌ (4) 无伤害信号发射；❌ (5) `VFXManager.hit_stop()` 存在但 Hero 从未调用 |
| ⚠️ | 2.2.2 | 全英雄拦截系统 | (1) Idle/Intercepting 状态机 (2) 怪物进入碰撞→Speed=0 (3) 英雄与怪物进入攻击/阻挡动画循环 (4) `block_count` 限制 (5) 超限后怪物透行 | ✅ (2) `set_paused(true)` 等效实现；✅ (4) `max_block_count`；❌ (1) 无状态机，`current_blocked_enemies` 只增不减无释放；❌ (3) 无动画循环；❌ (5) 超限无透行逻辑 |
| ❌ | 2.2.3 | 英雄属性动态计算 | `Final_Stat = (Base + Meta) * (1 + Star) * (1 + Buff)` 公式 | ❌ 公式未编码；`heroes_progression.json` 有基础属性但无计算逻辑 |
| ⚠️ | 2.3.1 | 怪物属性与波次导演 | (1) `move_speed` (2) `attack_dmg`(拦截伤害) (3) `wall_dmg` (4) 波次导演系统 (5) 血量随时间缩放 | ✅ (1)(3) 已导出；❌ (2) 无 `attack_dmg` 属性；❌ (4) BattleTest 仅有简单 Timer 生成，非正式波次导演；❌ (5) 无时间缩放 |
| ⚠️ | 2.3.2 | 城墙受击反馈与屏幕震动 | (1) 怪物到达底端→`wall_hit`→怪物销毁 (2) Camera2D 震动：intensity=5.0, 0.2s, randf 随机偏移 | ✅ (1) Enemy 已实现；❌ (2) 参数在 `battle_logic_config.json` 已定义，VFXManager 无震动方法，Camera2D 无震动代码 |
| ❌ | 2.3.3 | 死亡动画标准 | (1) HP≤0→禁用碰撞与 process (2) scale 1.0→0 (TRANS_BACK) (3) modulate:a→0 (4) position.y 微量上漂 (5) queue_free() + `enemy_died` 信号 | ❌ 全部未实现。Enemy 当前无 HP 扣除逻辑（无伤害来源），到达底线直接 `queue_free()`，无死亡动画，`enemy_died` 信号从未被 emit |

---

### 3. 卡牌系统与手牌管理 (Card & Hand Management)

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ✅ | 3.1.1 | 隐藏备用队列逻辑 | (1) FIFO 队列数据结构 (2) 手牌<5 时自动弹出队首 (3) Tween "飞入"动画 (4) 队列总数记录供 UI 使用 | ✅ (1)(2) `DeckManager` 完整实现；❌ (3) 无卡牌 UI，无飞入动画；⚠️ (4) `card_queue.size()` 存在但无显式 getter 供 UI 调用 |
| ❌ | 3.1.2 | 手牌槽位管理与 9+ 提示 | (1) 底部 5 固定槽位 (2) 每张手牌独立状态机（槽位/拖拽/选中） (3) 队列提示图标 (4) 9+ 规则（≤9 显示数字，>9 显示"9+"） | ❌ 全部未实现 |
| ⚠️ | 3.1.3 | 弃牌逻辑 | (1) [X] 按钮悬挂 (2) 点击触发销毁动画 (3) 释放槽位 (4) 触发自动填充 | ❌ (1)(2) 无 UI 与动画；✅ (3)(4) `DeckManager.discard_card()` + `_try_fill_hand()` 后端已就位 |
| ✅ | 3.2.1 | 强制触发判定 | (1) 监控 `field_hero_count`/`hand_count`/`queue_count` (2) 三项同时为 0 判定死锁 (3) 1s 扫描 + 英雄死亡即时触发 | ✅ (1)-(3) 完整实现 |
| ✅ | 3.2.2 | 冷却与状态控制 | (1) 10s 强制 CD (2) CD 期间不重复发牌 (3) "补给抵达"飘字提示 | ✅ (1)(2) 已实现；⚠️ (3) Toast UI 未实现，仅 emit 信号 |
| ⚠️ | 3.2.3 | 救援补给包内容 | (1) 5 张卡牌 (2) 仅限基础角色卡池 (3) 严禁效果卡/传说卡 | ✅ (1) 5 张；⚠️ (2) 代码写死 `"basic_hero"`，未读取 `battle_logic_config.json` 的 `package_content`（定义：shielder_01 x2 + gunner_01 x3），非数据驱动 |
| ❌ | 3.3.1 | 角色卡 (Character Cards) | Hero_ID、品质底版、初始 1 星、部署/合成逻辑 | ❌ 无 Card 类定义 |
| ❌ | 3.3.2 | 效果提升卡 (Upgrade Cards) | 流派锁定、每名英雄最多 5 次强化 | ❌ 未实现 |
| ❌ | 3.3.3 | 全局与传说卡 (Global/Legendary) | 全局效果（冰冻/免疫）、传说特技（色调反转/时间减缓） | ❌ 未实现 |

---

### 4. 战中合成与分支进化 (In-Battle Progression)

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ❌ | 4.1.1 | 星级提升规则与数值映射 | (1) 同 ID 拖拽合成 (2) 1-5 星上限 (3) 倍率：1.5x/2.2x/3.0x/4.5x (4) 继承 Buff + 恢复 20% HP | ❌ 全部未实现。`heroes_progression.json` 有 `battle_star_multipliers` 数据但无逻辑 |
| ❌ | 4.1.2 | 视觉进化表现标准 | (1) 每星 +10% 体型，5 星 1.4x (2) 1-3 星淡色呼吸光环 / 4 星粒子 / 5 星 Shader (3) Tween 回弹效果 | ❌ 全部未实现 |
| ❌ | 4.2.1 | 隐性分支锁定逻辑 | (1) 首次施加效果卡时锁定路径 (2) 路径 A/B 视觉区分 (3) 不可逆 | ❌ 未实现 |
| ❌ | 4.2.2 | 路线 A：攻速/防御流 | 每级 +15% 攻速或 +10% 减伤；机枪手"无限火力"、盾兵"反重力力场" | ❌ 未实现 |
| ❌ | 4.2.3 | 路线 B：爆发/反伤流 | 每级 +20% 暴击或 +25% 反伤；机枪手"核聚变弹头"、盾兵"荆棘圣盾" | ❌ 未实现 |

> 整个第 4 章为 **0%**

---

### 5. 局外养成系统 (Meta-Progression)

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ⚠️ | 5.1.1 | 碎片与金币消耗公式 | (1) `Cost_Shards = Base + (Level/5) * Step` (2) 金币非线性增长 (3) 上限 30 级 | ⚠️ `heroes_progression.json` 有 `shard_base_value`/`shard_step_multiplier` 数据；❌ 公式代码、金币公式、等级上限逻辑均未实现 |
| ❌ | 5.1.2 | 5 级间隔质变节点 | (1) 5/10/15/20/25/30 节点 (2) 手动点击激活 (3) 六边形图标 UI | ⚠️ JSON 有 6 个 `progression_milestones` 定义；❌ 激活逻辑与 UI 均未实现 |
| ❌ | 5.1.3 | 基础属性持久化增长 | 每级 ATK/HP/DEF 百分比提升，作为 `Base_Stat` 进入战斗公式 | ❌ 未实现 |
| ⚠️ | 5.2.1 | 城墙等级与 HP 加成 | `Wall_HP = Base_HP * (1 + Level * 0.15)` | ⚠️ `wall_config.json` 有 `base_hp=1000`, `growth_coefficient=0.15`；❌ 计算公式未编码 |
| ❌ | 5.2.2 | 升级金币消耗算法 | `Cost = base_cost * pow(level, cost_exponent)` (指数 1.2-1.5) | ❌ 未实现。`wall_config.json` 有 `upgrade_base_cost=500`, `cost_exponent=1.2` |
| ❌ | 5.3.1 | 副本结算奖励分配 | 金币收益（难度×星级倍率）+ 碎片掉落（首通+随机） | ❌ 未实现 |
| ⚠️ | 5.3.2 | 三星挑战目标体系 | (1) 1 星通关 / 2 星 HP>80% / 3 星 HP=100% (2) 三星 1.5x 金币 (3) 扫荡功能解锁 | ⚠️ `battle_logic_config.json` 有 `star_challenge_rewards`；❌ 通关判定、星级计算、扫荡均未实现 |

---

### 6. UI/UX 界面与交互 (Interface Design)

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ❌ | 6.1.1 | 五 Tab 底部导航 | (1) [商城]/[英雄]/[战斗]/[营地]/[公会] (2) 选中 Tab 图标上移+高亮 (3) 底部预留 100px 手势条避让 | ❌ 全部未实现 |
| ❌ | 6.1.2 | 英雄界面交互逻辑 | (1) 顶部 5 上阵槽位 (2) GridContainer 角色列表 (3) 长按拖拽上阵 (4) R/SR/SSR 品质底版 (5) 单点详情弹窗 | ❌ 全部未实现 |
| ❌ | 6.1.3 | 战斗首页（关卡选择） | (1) Boss 轮播图 (2) 普通/困难/地狱难度标识 (3) 星级预览与挑战目标 | ❌ 全部未实现 |
| ❌ | 6.2.1 | 通用弹窗基类 (BasePopup) | (1) #000000B2 半透明遮罩 (2) 80% 居中内容区 (3) Scale 0.8→1.0 + Modulate 0→1 TRANS_BACK 入场 (4) pivot_offset 动态居中 | ❌ 全部未实现 |
| ❌ | 6.2.2 | 栈式弹窗管理逻辑 | (1) 遮罩点击关闭 (2) 层级锁定阻断下层交互 (3) 缩小动画→queue_free() 关闭链 | ❌ 全部未实现 |
| ❌ | 6.2.3 | 结算界面奖励弹出 | (1) 全屏渐暗+胜利/失败标题 (2) 预实例奖励图标 (3) 0.15s 间隔依次弹出 (4) scale 0→1.0 带回弹+音效 | ❌ 全部未实现 |
| ❌ | 6.3.1 | 伤害飘字系统 | (1) 普通：白色+黑色描边，上漂 80px 渐隐 (2) 暴击：黄色 1.5x，Pop-up 缩放 (3) 0.6s 生命周期 | ❌ 全部未实现 |
| ❌ | 6.3.2 | 动态血条显示 | (1) 普通怪物不显示 (2) 精英受击显示 TextureProgressBar (3) Boss 常驻血条+残影扣除 (4) 未受击后渐隐 | ❌ 全部未实现 |
| ❌ | 6.3.3 | 系统反馈 Toast | (1) 死锁补给："补给抵达" 1.5s 渐隐 (2) 错误码红色跳动提示 | ❌ 全部未实现。`error_codes.json` 有错误码映射，`DataManager` 已加载，但无消费端 |

---

### 7. 数值表格与技术架构 (Technical Design)

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ❌ | 7.1.1 | 关卡配置表 (LevelResource) | level_id, difficulty_coefficient, wave_data, bg_texture, grid_tile_set, reward_pool | ❌ 未创建 `.tres`，无任何关卡数据文件 |
| ⚠️ | 7.1.2 | 英雄成长表 (HeroProgressionResource) | hero_id, base_atk/hp/def, block_count, level_costs, growth_curve, node_data | ⚠️ JSON 有 `hero_base_templates` + `battle_star_multipliers` + `progression_milestones`；❌ 缺少 `level_costs` 字典（每级金币/碎片消耗）、`growth_curve` 曲线、`node_data` 天赋节点完整数据 |
| ✅ | 7.1.3 | 错误码表 (ErrorCodeResource) | Dictionary[int, String]，范例：100/101/102/200 | ✅ `error_codes.json`（5 条），`DataManager._ready()` 自动加载至 `error_messages` |
| ⚠️ | 7.2.1 | 全局信号总线 | 大纲要求 5 个信号（见下方对照表） | ⚠️ 已实现 4 个，**缺少 1 个**，且已有信号的参数与大纲不一致 |

**§7.2.1 信号逐条对照：**

| 大纲定义 | 代码实现 | 匹配? |
|----------|----------|-------|
| `wall_hit(damage: int, current_hp: int)` | `wall_hit(damage: int)` | ❌ 缺少 `current_hp` 参数 |
| `enemy_died(global_pos: Vector2, enemy_type: String)` | `enemy_died(pos: Vector2)` | ❌ 缺少 `enemy_type` 参数 |
| `card_deployed(logic_pos: Vector2i, hero_data: HeroResource)` | `card_deployed(logic_pos: Vector2i, hero_id: String)` | ❌ 参数类型不匹配（`String` vs `HeroResource`） |
| `deadlock_rescue_triggered()` | `deadlock_rescue_triggered()` | ✅ 完全匹配 |
| `currency_changed(type: String, new_amount: int)` | — | ❌ **信号缺失** |

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ⚠️ | 7.2.2 | 数据持久化管理 | 大纲要求存储 4 类数据 | ⚠️ 见下方对照表 |

**§7.2.2 存储内容逐条对照：**

| 大纲要求 | 代码实现 | 匹配? |
|----------|----------|-------|
| 英雄等级字典 | 未存储 | ❌ |
| 关卡进度（星级） | 未存储 | ❌ |
| 城墙等级 | 未存储 | ❌ |
| 全局资源（金币/碎片总数） | 仅 `gold`，无碎片 | ❌ 不完整 |
| 文件路径 `user://save_v1.dat` | `user://save_game.dat` | ⚠️ 路径不一致 |
| AES 加密 `FileAccess.open_encrypted_with_pass()` | `ConfigFile.save_encrypted_pass()` / `load_encrypted_pass()` | ⚠️ 实现方式不同，但效果等价 |

| 状态 | 章节 | 内容 | 子项拆解 | 实际状态 |
|------|------|------|----------|----------|
| ❌ | 7.2.3 | 场景管理与异步切换 | (1) Loading Scene (2) `ResourceLoader.load_threaded_request` (3) `change_scene_to_packed` (4) `GameManager` 单例保持状态 | ❌ 全部未实现 |
| ⚠️ | 7.3 | 性能优化标准 | (1) 对象池：子弹/飘字/小怪 (2) 检测频率优化为 0.1s (3) 弹窗关闭后 Tween 断连/GC | ❌ (1) 无对象池；✅ (2) Hero 检测 0.1s；❌ (3) 无弹窗故无此需求 |

---

## 三、总体完成度汇总

### 按 Phase 统计

| Phase | 名称 | ✅ | ⚠️ | ❌ | 完成率 |
|-------|------|-----|-----|-----|--------|
| Phase 1 | 基础设施与数据契约 | 3 | 0 | 0 | **100%** |
| Phase 2 | 战斗实体与核心判定 | 0 | 3 | 0 | **~60%** |
| Phase 3 | 卡牌系统与手牌逻辑 | 1 | 0 | 1 | **50%** |
| Phase 4 | UI 表现与持久化 | 2 | 1 | 0 | **83%** |
| **合计** | | **6** | **4** | **1** | **~73%** |

### 按大纲章节统计

| 章节 | 名称 | 总条目 | ✅ | ⚠️ | ❌ | 完成率 |
|------|------|--------|-----|-----|-----|--------|
| §1 | 项目综述（可验证项） | 8 | 6 | 0 | 2 | 75% |
| §2 | 战斗系统逻辑 | 8 | 1 | 5 | 2 | 44% |
| §3 | 卡牌系统与手牌管理 | 9 | 3 | 2 | 4 | 44% |
| §4 | 战中合成与分支进化 | 5 | 0 | 0 | 5 | **0%** |
| §5 | 局外养成系统 | 7 | 0 | 3 | 4 | 21% |
| §6 | UI/UX 界面与交互 | 9 | 0 | 0 | 9 | **0%** |
| §7 | 数值表格与技术架构 | 8 | 1 | 5 | 2 | 44% |
| **合计** | | **54** | **11** | **15** | **28** | **34%** |

> 注：§7.2.1 与 §7.2.2 的逐条信号/存储对照分别算入 §7。

### 已完成的核心资产（12 项）

| # | 资产 | 文件 | 关键能力 |
|---|------|------|----------|
| 1 | 信号总线 | `Scripts/Global/GameEvents.gd` | 4 信号（参数待补全） |
| 2 | 坐标映射 | `Scripts/Logic/GridManager.gd` | 5x5 ↔ 屏幕 + Safe Area + clamp |
| 3 | 数据加载 | `Scripts/Global/DataManager.gd` | JSON 安全加载 + 错误码表 |
| 4 | 强制位移 | `Scripts/Managers/BattleManager.gd` | 递归挤压 + TRANS_QUART Tween |
| 5 | 队列管理 | `Scripts/Managers/DeckManager.gd` | FIFO + 自动填充 + 死锁救援 |
| 6 | 打击停顿 | `Scripts/Global/VFXManager.gd` | 引用计数 Hit Stop |
| 7 | 加密存档 | `Scripts/Global/SaveManager.gd` | AES ConfigFile（存储内容待扩充） |
| 8 | 怪物实体 | `Scripts/Entities/Enemy.gd` + `Scenes/Enemy.tscn` | 移动 + wall_hit（缺伤害/死亡） |
| 9 | 英雄实体 | `Scripts/Entities/Hero.gd` + `Scenes/Hero.tscn` | RayCast2D 拦截（缺攻击/状态机） |
| 10 | 安全区适配 | `Scripts/UI/SafeAreaContainer.gd` | 刘海屏 + 手势条避让 |
| 11 | 集成测试 | `Scripts/Test/BattleTest.gd` + `Scenes/BattleTest.tscn` | Phase 6 交互部署验证 |
| 12 | 配置数据 | `Data/*.json` (4 文件) | 战斗逻辑/英雄模板/城墙/错误码 |

### 最大的待办缺口（按优先级排列）

| 优先级 | 缺口 | 涉及大纲章节 | 说明 |
|--------|------|-------------|------|
| **P0** | 卡牌 UI 系统 | §3.1.2, §3.1.3, §6.1 | 手牌槽位、拖拽部署、底栏导航——连接 DeckManager 与玩家操作的唯一桥梁，无此则游戏不可玩 |
| **P0** | 英雄攻击伤害闭环 | §2.2.1, §2.3.3 | RayCast2D 已检测到敌人但无伤害输出、无死亡动画、`enemy_died` 信号从未触发 |
| **P1** | 信号参数补全 | §7.2.1 | 缺 `currency_changed` 信号，`wall_hit`/`enemy_died`/`card_deployed` 参数与大纲不一致 |
| **P1** | 合成与分支进化 | §4 | 核心差异化玩法，0% |
| **P1** | 屏幕震动 | §2.3.2 | VFXManager 有参数但无 Camera2D 震动代码，`wall_hit` 触发后无视觉反馈 |
| **P2** | 伤害飘字与血条 | §6.3.1, §6.3.2 | 战斗反馈基础，无此则玩家不知道发生了什么 |
| **P2** | 局外养成系统 | §5 | 数据表已有，升级/节点/城墙/结算逻辑全部缺失 |
| **P2** | 波次导演系统 | §2.3.1 | 目前仅有测试用 Timer，无正式关卡波次编排 |
| **P3** | 场景异步切换 | §7.2.3 | Loading Scene / ResourceLoader / GameManager |
| **P3** | 救援卡池数据驱动 | §3.2.3 | 代码写死 `"basic_hero"`，应改为读取 JSON `package_content` |
| **P3** | SaveManager 存储扩充 | §7.2.2 | 补充英雄等级/关卡星级/城墙等级/碎片 |
| **P3** | 对象池 | §7.3 | 子弹/飘字/小怪复用 |

---

*本文档由代码库实际状态与两份设计文档逐条交叉比对生成。*
