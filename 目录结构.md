game-demo-w/
├── Assets/        <-- 放你收集的 UI 参考图、美术素材、音效
├── Data/          <-- 放所有的 JSON 配置表（错误码、卡牌属性、抽卡概率）
    ├── heroes_progression.json     <-- 英雄基础与成长配置
    ├── wall_config.json            <-- 城墙养成配置表
    ├── battle_logic_config.json    <-- 战斗逻辑与死锁救援配置
    ├── error_codes.json            <-- 系统反馈与错误码表
├── Docs/           <-- 放你用 Markdown 写的游戏开发文档和策划案
├── Scenes/         <-- 放 Godot 的场景文件 (.tscn)
├── Scripts/        <-- 放所有的代码脚本 (.gd)
├── Excel/          <-- 原始表格
└── project.godot


提示词内容：
"为了极大地优化你的思考和搜索时间，我需要你为自己建立并维护一个专属的自动化项目地图系统。请执行以下初始化操作：
创建地图文件：在项目根目录创建一个名为 .ai_project_map.json 的隐藏文件。扫描当前项目的核心架构（重点是 Scripts, Scenes, Assets 目录结构、核心类名、状态机逻辑和资源映射规则），用你认为最高效、解析最快的数据结构（例如高密度 JSON）写入该文件。无需考虑人类可读性，只要你能瞬间解析即可。
建立自运转规则：在项目根目录创建或更新 CLAUDE.md 文件，将以下两条核心指令写入其中，作为你的永久系统规则：
【预读取规则】：在处理涉及文件搜索、架构修改、资源调用的任务时，优先读取 .ai_project_map.json。除非地图信息不足，否则绝对不要使用全局搜索工具遍历目录。
【自动更新规则】：在每次成功完成代码修改、新建文件或调整目录结构后，你必须自主判断并静默更新 .ai_project_map.json，保持你的地图永远是最新的。
初始化完成后，请用一句话告诉我系统已就绪即可，无需解释具体结构。"